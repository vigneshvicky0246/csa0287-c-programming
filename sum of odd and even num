#include<stdio.h>
int main(){
	int b,i,sum=0,sum1=0;
	printf("enter a value for n");
	scanf("%d",&b);
	for(i=0;i<=b;i++)
	{
		if(i%2==0)
		sum=sum+i;
		else
		sum1=sum1+i;
	}

 printf("%d\n",sum);
  printf("%d",sum1);
}
